name: Deploy to AWS - ReadProvider

on:
  push:
    branches:
      - test  # Se ejecutará solo cuando hagas push a la rama "test"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v3

      - name: Login en Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Construir y subir imagen a Docker Hub
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/projectawsstore-readprovider:latest .
          docker push ${{ secrets.DOCKER_USERNAME }}/projectawsstore-readprovider:latest

      - name: Conectar a AWS EC2 y desplegar
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST_READ }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # Detener y eliminar el contenedor anterior si existe
            sudo docker stop read-provider || true
            sudo docker rm read-provider || true
            
            # Descargar la nueva versión desde Docker Hub
            sudo docker pull ${{ secrets.DOCKER_USERNAME }}/projectawsstore-readprovider:latest
            
            # Ejecutar el contenedor con el nombre correcto y los puertos adecuados
            sudo docker run -d -p 5003:5003 -p 4003:4003 --name read-provider ${{ secrets.DOCKER_USERNAME }}/projectawsstore-readprovider:latest
